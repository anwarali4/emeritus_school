<div class="col-xs-12 col md-12">
  <div class="card">
    <div class="header">
      <h4 class="title">
        <i class="pe-7s-ribbon"></i>
        Listing Enrollent
      </h4>
      <hr/>
      <%= link_to new_student_enrollment_path, class: 'btn btn-primary btn-fill' do %>
        <i class="fa fa-plus-square"></i>
        Add To New Batch
      <% end %>
      <br/>
      <br/>
    </div>
    <div class="content table-responsive table-full-width">
      <table class="table table-hover table-striped">
        <thead>
          <tr>
            <th>Batch</th>
            <th>Course</th>
            <th>Course</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% @enrollments.each do |enrollment| %>
            <tr>
              <td>
                <%= enrollment.batch.name %>
              </td>
              <td>
                <%= enrollment.batch.course.name %>
              </td>
              <td>
                <%= enrollment.batch.course.school.name %>
              </td>
              <td>
                <%= link_to student_enrollment_path(enrollment), class: 'btn btn-info btn-sm' do %>
                  <i class="fa fa-eye"></i>
                  Show
                <% end %>
                <% if enrollment.status == 'pending' %>
                  <%= link_to student_enrollment_path(id: enrollment.id, status: 'approve'), method: :delete, class: 'btn btn-primary btn-sm' do %>
                    <i class="fa fa-edit"></i>
                    WithDraw
                  <% end %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
    <br/>
  </div>
</div>
